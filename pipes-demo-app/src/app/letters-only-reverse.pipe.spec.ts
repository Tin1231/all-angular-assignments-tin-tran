import { LettersOnlyReversePipe } from './letters-only-reverse.pipe';

describe('LettersOnlyReversePipe', () => {
  it('create an instance', () => {
    const pipe = new LettersOnlyReversePipe();
    expect(pipe).toBeTruthy();
  });
});
