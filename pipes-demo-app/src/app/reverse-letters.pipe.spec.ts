import { ReverseLettersPipe } from './reverse-letters.pipe';

describe('ReverseLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
